
import './App.css';
import io from 'socket.io-client';

const socketURL = 'http://localhost:3009';
socketURL.connect(socketURL);

function App() {

  return (
    <div className="App">
     
    </div>
  );
}

export default App;
